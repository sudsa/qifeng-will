nacos:
  server-addr: 10.39.80.39:8848
  namespace: qifeng
spring:
  application:
    name: user-server
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr}
        file-extension: yml
        namespace: ${nacos.namespace}

domain: user-server



