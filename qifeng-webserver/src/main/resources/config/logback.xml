<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
	<!-- 日志存放的路径 ,默认为tomcat下面的logs -->
	<!--<property name="USER_HOME" value="${catalina.home:-.}" />-->
	<!--<property name="USER_HOME" value="d:/logdir/" />-->
	<property name="USER_HOME" value="/data/bpaaslog" />
	<springProperty scope="context" name="flumeAgents" source="flume.agents"/>
	<springProperty scope="context" name="flumeActionAgents" source="flume.action"/>
	<springProperty scope="context" name="flumeuser" source="flume.user"/>
	<springProperty scope="context" name="domain" source="domain"/>
	<!-- 输出到窗口 -->
	<appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%date{HH:mm:ss.SSS} %-5level %logger{50} [%thread]- %msg%n</pattern>
			<charset>UTF-8</charset>
		</encoder>
	</appender>

	<appender name="logfile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- 按天分文件夹存放 ，并压缩 -->
			<fileNamePattern>${USER_HOME}/logs/${domain}/%d{yyyy-MM-dd}/log.%i.txt.gz
			</fileNamePattern>
			<timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<!-- 每个文件的最大值 5MB,超过则编号,生成新文件 -->
				<maxFileSize>5MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
			<!-- 例如，假设每天滚动，且 maxHistory 是 6，则只 保留最近 6 天的归档文件，删除之前的文件。 -->
			<maxHistory>60</maxHistory>
		</rollingPolicy>
		<encoder>
			<pattern>%date{HH:mm:ss.SSS} %-5level %logger{40} - %msg%n</pattern>
			<charset>UTF-8</charset>
		</encoder>
	</appender>

	<logger name="java.sql.Connection" level="INFO" />
	<logger name="java.sql.Statement" level="INFO" />
	<logger name="java.sql.PreparedStatement" level="INFO" />

	<logger name="org.springframework" level="INFO" />

	<logger name="com.whty" level="DEBUG" />
	
    <!-- <logger name="com.whty.framework.log.service.impl.LogServiceImpl"  level="TRACE" >
		<appender-ref ref="flume" />
	</logger> -->
	

	<root level="INFO">
		<appender-ref ref="stdout" />
		<appender-ref ref="logfile" />
	</root>
</configuration>